# Aegis 2.0 — Docker Compose (占位，M3 完善)
version: "3.8"

services:
  # 占位：后端服务
  # backend:
  #   build: ./backend
  #   ports:
  #     - "8000:8000"
  #   env_file:
  #     - ./backend/.env
  #   volumes:
  #     - ./backend/data:/app/data
