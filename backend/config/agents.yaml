# Aegis 2.0 — Agent Manifest Configuration
# 每个 Agent 的注册信息在此声明
# 新增 Agent 必须包含: name / version / requires / provides / tags / llm_dependency / parallel_group / pipeline_mode / enabled

agents:
  # 示例 Agent 条目（M1 各分支填充）
  # data_harvester:
  #   name: data_harvester
  #   version: "0.1.0"
  #   requires: [tickers]
  #   provides: [market_data, macro_data]
  #   tags: [data, market, macro]
  #   llm_dependency: false
  #   parallel_group: null
  #   pipeline_mode: both
  #   enabled: true
